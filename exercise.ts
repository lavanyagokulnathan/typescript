# primitive types
let age : number = 18;

# Interface - used to create custom types
interface Contact {
  id: number,
  name: string,
  birthDate: Date
}

interface Address {
  line1: string,
  line2: string,
  province: string,
  postalCode: string
}

